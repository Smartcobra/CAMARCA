# Scripts

Use package entrypoints instead of ad-hoc scripts whenever possible:

- `uv run camarca ...`
- `uv run camarca-hybrid ...`
- `uv run camarca-convert`
- `uv run camarca-convert-partitioned`
